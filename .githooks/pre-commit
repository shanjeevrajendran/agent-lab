#!/bin/sh
# Block the commit if the evals fail. Enable with: git config core.hooksPath .githooks
cd "$(git rev-parse --show-toplevel)" || exit 1
python3 evals.py || {
  echo "pre-commit: evals failed, commit blocked." >&2
  exit 1
}
