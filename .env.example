# Copy this file to .env (git-ignored) and fill it in. Never put real keys in this file.
# Nothing reads these yet: the current adapters are stubs. They are placeholders for real ones.

# CLOUD_API_KEY=your-key-here
# LOCAL_MODEL_URL=http://localhost:11434
